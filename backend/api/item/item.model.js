class Item {
    constructor(codigo, descricao, data_criacao, ativo){
        this.codigo = codigo
        this.descricao = descricao
        this.data_criacao = data_criacao
        this.ativo = ativo
    }
}